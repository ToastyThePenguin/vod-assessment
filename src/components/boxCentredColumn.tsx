import { Box } from '@mui/material';
import { styled } from '@mui/system';

const BoxCentredColumn = styled(Box)({
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'center'
});

export default BoxCentredColumn;